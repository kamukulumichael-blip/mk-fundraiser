name: Email Brief Dispatch

on:
  schedule:
    - cron: '0 3 * * *'   # 06:00 EAT (morning slot) — skips if research.yml already sent Mon morning
    - cron: '0 10 * * *'  # 13:00 EAT (afternoon slot)
  workflow_dispatch:       # Manual trigger — useful for "send now"

permissions:
  contents: write

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4.2.2

      - uses: actions/setup-python@v5.3.0
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install anthropic requests json-repair

      - name: Dispatch email brief
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          GITHUB_EVENT_NAME: ${{ github.event_name }}
        run: python email_dispatch.py

      - name: Commit updated email config (last_sent)
        run: |
          git config user.name  "Raisr Email Bot"
          git config user.email "actions@github.com"
          git add data/email_config.json
          git diff --cached --quiet && echo "No changes to commit" || git commit -m "email: brief dispatched [skip pages]"
          git push
